// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package roo.nw.repository;

import static org.assertj.core.api.Assertions.assertThat;

import io.springlets.data.domain.GlobalSearch;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Before;
import org.junit.Rule;
import org.junit.Test;
import org.junit.rules.ExpectedException;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;
import org.springframework.context.annotation.Import;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.PageRequest;
import org.springframework.test.context.junit4.SpringRunner;
import roo.nw.application.config.SpringDataJpaDetachableRepositoryConfiguration;
import roo.nw.model.Employee;
import roo.nw.model.dod.DataOnDemandConfiguration;
import roo.nw.model.dod.EmployeeDataOnDemand;
import roo.nw.repository.EmployeeRepository;
import roo.nw.repository.EmployeeRepositoryIT;

privileged aspect EmployeeRepositoryIT_Roo_RepositoryJpaIntegrationTest {
    
    declare @type: EmployeeRepositoryIT: @RunWith(SpringRunner.class);
    
    declare @type: EmployeeRepositoryIT: @DataJpaTest;
    
    declare @type: EmployeeRepositoryIT: @Import({ DataOnDemandConfiguration.class, SpringDataJpaDetachableRepositoryConfiguration.class });
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    @Rule
    public ExpectedException EmployeeRepositoryIT.thrown = ExpectedException.none();
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    @Autowired
    private EmployeeRepository EmployeeRepositoryIT.repository;
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    @Autowired
    private EmployeeDataOnDemand EmployeeRepositoryIT.dod;
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return EmployeeRepository
     */
    public EmployeeRepository EmployeeRepositoryIT.getRepository() {
        return repository;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return EmployeeDataOnDemand
     */
    public EmployeeDataOnDemand EmployeeRepositoryIT.getDod() {
        return dod;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Before
    public void EmployeeRepositoryIT.checkDataOnDemandHasInitializedCorrectly() {
        assertThat(getDod().getRandomEmployee())
            .as("Check data on demand for 'Employee' initializes correctly by getting a random Employee")
            .isNotNull();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void EmployeeRepositoryIT.countShouldReturnExpectedValue() {
        // Verify
        assertThat(getRepository().count()).as("Check there are available 'Employee' entries").isGreaterThan(0);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void EmployeeRepositoryIT.findOneShouldReturnExistingEmployee() {
        // Setup
        Long id = getRandomEmployeeId();
        
        // Exercise
        Employee employee = getRepository().findOne(id);
        
        // Verify
        assertThat(employee).as("Check that findOne illegally returned null for id %s", id).isNotNull();
        assertThat(id).as("Check the identifier of the found 'Employee' is the same used to look for it")
            .isEqualTo(employee.getId());
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void EmployeeRepositoryIT.findAllShouldReturnAllEmployees() {
        // Setup
        assertThat(getRepository().count())
            .as("Check the number of entries is not too big (250 entries). "
                + "If it is, please review the tests so it doesn't take too long to run them")
            .isLessThan(250);
        
        // Exercise
        List<Employee> result = getRepository().findAll();
        
        // Verify
        assertThat(result).as("Check 'findAll' returns a not null list of entries").isNotNull();
        assertThat(result.size()).as("Check 'findAll' returns a not empty list of entries")
            .isGreaterThan(0);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void EmployeeRepositoryIT.persistShouldGenerateIdValue() {
        // Setup
        // Exercise
        Employee employee = getDod().getNewRandomTransientEmployee();
        
        // Verify
        assertThat(employee).as("Check the Data on demand generated a new non null 'Employee'").isNotNull();
        assertThat(employee.getId()).as("Check the Data on demand generated a new 'Employee' whose id is null")
            .isNull();
        try {
            employee = getRepository().saveAndFlush(employee);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter
                .hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".")
                    .append(cv.getPropertyPath()).append(": ").append(cv.getMessage())
                    .append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        assertThat(employee.getId()).as("Check a 'Employee' (%s) id is not null after been persisted", employee)
            .isNotNull();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void EmployeeRepositoryIT.deleteShouldMakeEmployeeUnavailable() {
        // Setup
        Long id = getRandomEmployeeId();
        Employee employee = getRepository().findOne(id);
        
        // Exercise
        getRepository().delete(employee);
        
        // Verify
        assertThat(getRepository().findOne(id))
        .as("Check the deleted 'Employee' %s is no longer available with 'findOne'", employee).isNull();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void EmployeeRepositoryIT.findAllCustomNotFilteredNotPagedShouldReturnAllEmployees() {
        // Exercise
        Page<Employee> all = getRepository().findAll((GlobalSearch) null, new PageRequest(0, getDod().getSize()));
        
        // Verify
        assertThat(all.getNumberOfElements())
            .as("Check 'findAll' with null 'GlobalSearch' and no pagination returns all entries")
            .isEqualTo(getDod().getSize());
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void EmployeeRepositoryIT.findAllCustomNotFilteredPagedShouldReturnAEmployeesPage() {
        // Exercise
        Page<Employee> all = getRepository().findAll((GlobalSearch) null, new PageRequest(0, 3));
        
        // Verify
        assertThat(all.getNumberOfElements())
            .as("Check result number is not greater than the page size").isLessThanOrEqualTo(3);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return Long
     */
    private Long EmployeeRepositoryIT.getRandomEmployeeId() {
        Employee employee = getDod().getRandomEmployee();
        Long id = employee.getId();
        assertThat(id).as("Check the Data on demand generated a 'Employee' with an identifier").isNotNull();
        return id;
    }
    
}
