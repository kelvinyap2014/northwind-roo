// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package roo.nw.repository;

import static org.assertj.core.api.Assertions.assertThat;

import io.springlets.data.domain.GlobalSearch;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Before;
import org.junit.Rule;
import org.junit.Test;
import org.junit.rules.ExpectedException;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;
import org.springframework.context.annotation.Import;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.PageRequest;
import org.springframework.test.context.junit4.SpringRunner;
import roo.nw.application.config.SpringDataJpaDetachableRepositoryConfiguration;
import roo.nw.model.SoldProduct;
import roo.nw.model.dod.DataOnDemandConfiguration;
import roo.nw.model.dod.SoldProductDataOnDemand;
import roo.nw.repository.SoldProductRepository;
import roo.nw.repository.SoldProductRepositoryIT;

privileged aspect SoldProductRepositoryIT_Roo_RepositoryJpaIntegrationTest {
    
    declare @type: SoldProductRepositoryIT: @RunWith(SpringRunner.class);
    
    declare @type: SoldProductRepositoryIT: @DataJpaTest;
    
    declare @type: SoldProductRepositoryIT: @Import({ DataOnDemandConfiguration.class, SpringDataJpaDetachableRepositoryConfiguration.class });
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    @Rule
    public ExpectedException SoldProductRepositoryIT.thrown = ExpectedException.none();
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    @Autowired
    private SoldProductRepository SoldProductRepositoryIT.repository;
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    @Autowired
    private SoldProductDataOnDemand SoldProductRepositoryIT.dod;
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return SoldProductRepository
     */
    public SoldProductRepository SoldProductRepositoryIT.getRepository() {
        return repository;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return SoldProductDataOnDemand
     */
    public SoldProductDataOnDemand SoldProductRepositoryIT.getDod() {
        return dod;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Before
    public void SoldProductRepositoryIT.checkDataOnDemandHasInitializedCorrectly() {
        assertThat(getDod().getRandomSoldProduct())
            .as("Check data on demand for 'SoldProduct' initializes correctly by getting a random SoldProduct")
            .isNotNull();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void SoldProductRepositoryIT.countShouldReturnExpectedValue() {
        // Verify
        assertThat(getRepository().count()).as("Check there are available 'SoldProduct' entries").isGreaterThan(0);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void SoldProductRepositoryIT.findOneShouldReturnExistingSoldProduct() {
        // Setup
        Long id = getRandomSoldProductId();
        
        // Exercise
        SoldProduct soldProduct = getRepository().findOne(id);
        
        // Verify
        assertThat(soldProduct).as("Check that findOne illegally returned null for id %s", id).isNotNull();
        assertThat(id).as("Check the identifier of the found 'SoldProduct' is the same used to look for it")
            .isEqualTo(soldProduct.getId());
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void SoldProductRepositoryIT.findAllShouldReturnAllSoldProducts() {
        // Setup
        assertThat(getRepository().count())
            .as("Check the number of entries is not too big (250 entries). "
                + "If it is, please review the tests so it doesn't take too long to run them")
            .isLessThan(250);
        
        // Exercise
        List<SoldProduct> result = getRepository().findAll();
        
        // Verify
        assertThat(result).as("Check 'findAll' returns a not null list of entries").isNotNull();
        assertThat(result.size()).as("Check 'findAll' returns a not empty list of entries")
            .isGreaterThan(0);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void SoldProductRepositoryIT.persistShouldGenerateIdValue() {
        // Setup
        // Exercise
        SoldProduct soldProduct = getDod().getNewRandomTransientSoldProduct();
        
        // Verify
        assertThat(soldProduct).as("Check the Data on demand generated a new non null 'SoldProduct'").isNotNull();
        assertThat(soldProduct.getId()).as("Check the Data on demand generated a new 'SoldProduct' whose id is null")
            .isNull();
        try {
            soldProduct = getRepository().saveAndFlush(soldProduct);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter
                .hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".")
                    .append(cv.getPropertyPath()).append(": ").append(cv.getMessage())
                    .append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        assertThat(soldProduct.getId()).as("Check a 'SoldProduct' (%s) id is not null after been persisted", soldProduct)
            .isNotNull();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void SoldProductRepositoryIT.deleteShouldMakeSoldProductUnavailable() {
        // Setup
        Long id = getRandomSoldProductId();
        SoldProduct soldProduct = getRepository().findOne(id);
        
        // Exercise
        getRepository().delete(soldProduct);
        
        // Verify
        assertThat(getRepository().findOne(id))
        .as("Check the deleted 'SoldProduct' %s is no longer available with 'findOne'", soldProduct).isNull();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void SoldProductRepositoryIT.findAllCustomNotFilteredNotPagedShouldReturnAllSoldProducts() {
        // Exercise
        Page<SoldProduct> all = getRepository().findAll((GlobalSearch) null, new PageRequest(0, getDod().getSize()));
        
        // Verify
        assertThat(all.getNumberOfElements())
            .as("Check 'findAll' with null 'GlobalSearch' and no pagination returns all entries")
            .isEqualTo(getDod().getSize());
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void SoldProductRepositoryIT.findAllCustomNotFilteredPagedShouldReturnASoldProductsPage() {
        // Exercise
        Page<SoldProduct> all = getRepository().findAll((GlobalSearch) null, new PageRequest(0, 3));
        
        // Verify
        assertThat(all.getNumberOfElements())
            .as("Check result number is not greater than the page size").isLessThanOrEqualTo(3);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return Long
     */
    private Long SoldProductRepositoryIT.getRandomSoldProductId() {
        SoldProduct soldProduct = getDod().getRandomSoldProduct();
        Long id = soldProduct.getId();
        assertThat(id).as("Check the Data on demand generated a 'SoldProduct' with an identifier").isNotNull();
        return id;
    }
    
}
