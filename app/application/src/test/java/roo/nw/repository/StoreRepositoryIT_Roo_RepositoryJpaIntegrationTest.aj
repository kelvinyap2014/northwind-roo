// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package roo.nw.repository;

import static org.assertj.core.api.Assertions.assertThat;

import io.springlets.data.domain.GlobalSearch;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Before;
import org.junit.Rule;
import org.junit.Test;
import org.junit.rules.ExpectedException;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;
import org.springframework.context.annotation.Import;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.PageRequest;
import org.springframework.test.context.junit4.SpringRunner;
import roo.nw.application.config.SpringDataJpaDetachableRepositoryConfiguration;
import roo.nw.model.Store;
import roo.nw.model.dod.DataOnDemandConfiguration;
import roo.nw.model.dod.StoreDataOnDemand;
import roo.nw.repository.StoreRepository;
import roo.nw.repository.StoreRepositoryIT;

privileged aspect StoreRepositoryIT_Roo_RepositoryJpaIntegrationTest {
    
    declare @type: StoreRepositoryIT: @RunWith(SpringRunner.class);
    
    declare @type: StoreRepositoryIT: @DataJpaTest;
    
    declare @type: StoreRepositoryIT: @Import({ DataOnDemandConfiguration.class, SpringDataJpaDetachableRepositoryConfiguration.class });
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    @Rule
    public ExpectedException StoreRepositoryIT.thrown = ExpectedException.none();
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    @Autowired
    private StoreRepository StoreRepositoryIT.repository;
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    @Autowired
    private StoreDataOnDemand StoreRepositoryIT.dod;
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return StoreRepository
     */
    public StoreRepository StoreRepositoryIT.getRepository() {
        return repository;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return StoreDataOnDemand
     */
    public StoreDataOnDemand StoreRepositoryIT.getDod() {
        return dod;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Before
    public void StoreRepositoryIT.checkDataOnDemandHasInitializedCorrectly() {
        assertThat(getDod().getRandomStore())
            .as("Check data on demand for 'Store' initializes correctly by getting a random Store")
            .isNotNull();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void StoreRepositoryIT.countShouldReturnExpectedValue() {
        // Verify
        assertThat(getRepository().count()).as("Check there are available 'Store' entries").isGreaterThan(0);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void StoreRepositoryIT.findOneShouldReturnExistingStore() {
        // Setup
        Long id = getRandomStoreId();
        
        // Exercise
        Store store = getRepository().findOne(id);
        
        // Verify
        assertThat(store).as("Check that findOne illegally returned null for id %s", id).isNotNull();
        assertThat(id).as("Check the identifier of the found 'Store' is the same used to look for it")
            .isEqualTo(store.getId());
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void StoreRepositoryIT.findAllShouldReturnAllStores() {
        // Setup
        assertThat(getRepository().count())
            .as("Check the number of entries is not too big (250 entries). "
                + "If it is, please review the tests so it doesn't take too long to run them")
            .isLessThan(250);
        
        // Exercise
        List<Store> result = getRepository().findAll();
        
        // Verify
        assertThat(result).as("Check 'findAll' returns a not null list of entries").isNotNull();
        assertThat(result.size()).as("Check 'findAll' returns a not empty list of entries")
            .isGreaterThan(0);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void StoreRepositoryIT.persistShouldGenerateIdValue() {
        // Setup
        // Exercise
        Store store = getDod().getNewRandomTransientStore();
        
        // Verify
        assertThat(store).as("Check the Data on demand generated a new non null 'Store'").isNotNull();
        assertThat(store.getId()).as("Check the Data on demand generated a new 'Store' whose id is null")
            .isNull();
        try {
            store = getRepository().saveAndFlush(store);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter
                .hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".")
                    .append(cv.getPropertyPath()).append(": ").append(cv.getMessage())
                    .append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        assertThat(store.getId()).as("Check a 'Store' (%s) id is not null after been persisted", store)
            .isNotNull();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void StoreRepositoryIT.deleteShouldMakeStoreUnavailable() {
        // Setup
        Long id = getRandomStoreId();
        Store store = getRepository().findOne(id);
        
        // Exercise
        getRepository().delete(store);
        
        // Verify
        assertThat(getRepository().findOne(id))
        .as("Check the deleted 'Store' %s is no longer available with 'findOne'", store).isNull();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void StoreRepositoryIT.findAllCustomNotFilteredNotPagedShouldReturnAllStores() {
        // Exercise
        Page<Store> all = getRepository().findAll((GlobalSearch) null, new PageRequest(0, getDod().getSize()));
        
        // Verify
        assertThat(all.getNumberOfElements())
            .as("Check 'findAll' with null 'GlobalSearch' and no pagination returns all entries")
            .isEqualTo(getDod().getSize());
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void StoreRepositoryIT.findAllCustomNotFilteredPagedShouldReturnAStoresPage() {
        // Exercise
        Page<Store> all = getRepository().findAll((GlobalSearch) null, new PageRequest(0, 3));
        
        // Verify
        assertThat(all.getNumberOfElements())
            .as("Check result number is not greater than the page size").isLessThanOrEqualTo(3);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return Long
     */
    private Long StoreRepositoryIT.getRandomStoreId() {
        Store store = getDod().getRandomStore();
        Long id = store.getId();
        assertThat(id).as("Check the Data on demand generated a 'Store' with an identifier").isNotNull();
        return id;
    }
    
}
