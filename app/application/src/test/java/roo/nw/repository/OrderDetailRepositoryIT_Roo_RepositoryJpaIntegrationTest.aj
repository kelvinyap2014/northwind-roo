// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package roo.nw.repository;

import static org.assertj.core.api.Assertions.assertThat;

import io.springlets.data.domain.GlobalSearch;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Before;
import org.junit.Rule;
import org.junit.Test;
import org.junit.rules.ExpectedException;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;
import org.springframework.context.annotation.Import;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.PageRequest;
import org.springframework.test.context.junit4.SpringRunner;
import roo.nw.application.config.SpringDataJpaDetachableRepositoryConfiguration;
import roo.nw.model.OrderDetail;
import roo.nw.model.dod.DataOnDemandConfiguration;
import roo.nw.model.dod.OrderDetailDataOnDemand;
import roo.nw.repository.OrderDetailRepository;
import roo.nw.repository.OrderDetailRepositoryIT;

privileged aspect OrderDetailRepositoryIT_Roo_RepositoryJpaIntegrationTest {
    
    declare @type: OrderDetailRepositoryIT: @RunWith(SpringRunner.class);
    
    declare @type: OrderDetailRepositoryIT: @DataJpaTest;
    
    declare @type: OrderDetailRepositoryIT: @Import({ DataOnDemandConfiguration.class, SpringDataJpaDetachableRepositoryConfiguration.class });
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    @Rule
    public ExpectedException OrderDetailRepositoryIT.thrown = ExpectedException.none();
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    @Autowired
    private OrderDetailRepository OrderDetailRepositoryIT.repository;
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    @Autowired
    private OrderDetailDataOnDemand OrderDetailRepositoryIT.dod;
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return OrderDetailRepository
     */
    public OrderDetailRepository OrderDetailRepositoryIT.getRepository() {
        return repository;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return OrderDetailDataOnDemand
     */
    public OrderDetailDataOnDemand OrderDetailRepositoryIT.getDod() {
        return dod;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Before
    public void OrderDetailRepositoryIT.checkDataOnDemandHasInitializedCorrectly() {
        assertThat(getDod().getRandomOrderDetail())
            .as("Check data on demand for 'OrderDetail' initializes correctly by getting a random OrderDetail")
            .isNotNull();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void OrderDetailRepositoryIT.countShouldReturnExpectedValue() {
        // Verify
        assertThat(getRepository().count()).as("Check there are available 'OrderDetail' entries").isGreaterThan(0);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void OrderDetailRepositoryIT.findOneShouldReturnExistingOrderDetail() {
        // Setup
        Long id = getRandomOrderDetailId();
        
        // Exercise
        OrderDetail orderDetail = getRepository().findOne(id);
        
        // Verify
        assertThat(orderDetail).as("Check that findOne illegally returned null for id %s", id).isNotNull();
        assertThat(id).as("Check the identifier of the found 'OrderDetail' is the same used to look for it")
            .isEqualTo(orderDetail.getId());
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void OrderDetailRepositoryIT.findAllShouldReturnAllOrderDetails() {
        // Setup
        assertThat(getRepository().count())
            .as("Check the number of entries is not too big (250 entries). "
                + "If it is, please review the tests so it doesn't take too long to run them")
            .isLessThan(250);
        
        // Exercise
        List<OrderDetail> result = getRepository().findAll();
        
        // Verify
        assertThat(result).as("Check 'findAll' returns a not null list of entries").isNotNull();
        assertThat(result.size()).as("Check 'findAll' returns a not empty list of entries")
            .isGreaterThan(0);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void OrderDetailRepositoryIT.persistShouldGenerateIdValue() {
        // Setup
        // Exercise
        OrderDetail orderDetail = getDod().getNewRandomTransientOrderDetail();
        
        // Verify
        assertThat(orderDetail).as("Check the Data on demand generated a new non null 'OrderDetail'").isNotNull();
        assertThat(orderDetail.getId()).as("Check the Data on demand generated a new 'OrderDetail' whose id is null")
            .isNull();
        try {
            orderDetail = getRepository().saveAndFlush(orderDetail);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter
                .hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".")
                    .append(cv.getPropertyPath()).append(": ").append(cv.getMessage())
                    .append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        assertThat(orderDetail.getId()).as("Check a 'OrderDetail' (%s) id is not null after been persisted", orderDetail)
            .isNotNull();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void OrderDetailRepositoryIT.deleteShouldMakeOrderDetailUnavailable() {
        // Setup
        Long id = getRandomOrderDetailId();
        OrderDetail orderDetail = getRepository().findOne(id);
        
        // Exercise
        getRepository().delete(orderDetail);
        
        // Verify
        assertThat(getRepository().findOne(id))
        .as("Check the deleted 'OrderDetail' %s is no longer available with 'findOne'", orderDetail).isNull();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void OrderDetailRepositoryIT.findAllCustomNotFilteredNotPagedShouldReturnAllOrderDetails() {
        // Exercise
        Page<OrderDetail> all = getRepository().findAll((GlobalSearch) null, new PageRequest(0, getDod().getSize()));
        
        // Verify
        assertThat(all.getNumberOfElements())
            .as("Check 'findAll' with null 'GlobalSearch' and no pagination returns all entries")
            .isEqualTo(getDod().getSize());
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void OrderDetailRepositoryIT.findAllCustomNotFilteredPagedShouldReturnAOrderDetailsPage() {
        // Exercise
        Page<OrderDetail> all = getRepository().findAll((GlobalSearch) null, new PageRequest(0, 3));
        
        // Verify
        assertThat(all.getNumberOfElements())
            .as("Check result number is not greater than the page size").isLessThanOrEqualTo(3);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return Long
     */
    private Long OrderDetailRepositoryIT.getRandomOrderDetailId() {
        OrderDetail orderDetail = getDod().getRandomOrderDetail();
        Long id = orderDetail.getId();
        assertThat(id).as("Check the Data on demand generated a 'OrderDetail' with an identifier").isNotNull();
        return id;
    }
    
}
