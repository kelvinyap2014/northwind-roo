// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package roo.nw.repository;

import static org.assertj.core.api.Assertions.assertThat;

import io.springlets.data.domain.GlobalSearch;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Before;
import org.junit.Rule;
import org.junit.Test;
import org.junit.rules.ExpectedException;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;
import org.springframework.context.annotation.Import;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.PageRequest;
import org.springframework.test.context.junit4.SpringRunner;
import roo.nw.application.config.SpringDataJpaDetachableRepositoryConfiguration;
import roo.nw.model.Report;
import roo.nw.model.dod.DataOnDemandConfiguration;
import roo.nw.model.dod.ReportDataOnDemand;
import roo.nw.repository.ReportRepository;
import roo.nw.repository.ReportRepositoryIT;

privileged aspect ReportRepositoryIT_Roo_RepositoryJpaIntegrationTest {
    
    declare @type: ReportRepositoryIT: @RunWith(SpringRunner.class);
    
    declare @type: ReportRepositoryIT: @DataJpaTest;
    
    declare @type: ReportRepositoryIT: @Import({ DataOnDemandConfiguration.class, SpringDataJpaDetachableRepositoryConfiguration.class });
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    @Rule
    public ExpectedException ReportRepositoryIT.thrown = ExpectedException.none();
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    @Autowired
    private ReportRepository ReportRepositoryIT.repository;
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    @Autowired
    private ReportDataOnDemand ReportRepositoryIT.dod;
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return ReportRepository
     */
    public ReportRepository ReportRepositoryIT.getRepository() {
        return repository;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return ReportDataOnDemand
     */
    public ReportDataOnDemand ReportRepositoryIT.getDod() {
        return dod;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Before
    public void ReportRepositoryIT.checkDataOnDemandHasInitializedCorrectly() {
        assertThat(getDod().getRandomReport())
            .as("Check data on demand for 'Report' initializes correctly by getting a random Report")
            .isNotNull();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void ReportRepositoryIT.countShouldReturnExpectedValue() {
        // Verify
        assertThat(getRepository().count()).as("Check there are available 'Report' entries").isGreaterThan(0);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void ReportRepositoryIT.findOneShouldReturnExistingReport() {
        // Setup
        Long id = getRandomReportId();
        
        // Exercise
        Report report = getRepository().findOne(id);
        
        // Verify
        assertThat(report).as("Check that findOne illegally returned null for id %s", id).isNotNull();
        assertThat(id).as("Check the identifier of the found 'Report' is the same used to look for it")
            .isEqualTo(report.getId());
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void ReportRepositoryIT.findAllShouldReturnAllReports() {
        // Setup
        assertThat(getRepository().count())
            .as("Check the number of entries is not too big (250 entries). "
                + "If it is, please review the tests so it doesn't take too long to run them")
            .isLessThan(250);
        
        // Exercise
        List<Report> result = getRepository().findAll();
        
        // Verify
        assertThat(result).as("Check 'findAll' returns a not null list of entries").isNotNull();
        assertThat(result.size()).as("Check 'findAll' returns a not empty list of entries")
            .isGreaterThan(0);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void ReportRepositoryIT.persistShouldGenerateIdValue() {
        // Setup
        // Exercise
        Report report = getDod().getNewRandomTransientReport();
        
        // Verify
        assertThat(report).as("Check the Data on demand generated a new non null 'Report'").isNotNull();
        assertThat(report.getId()).as("Check the Data on demand generated a new 'Report' whose id is null")
            .isNull();
        try {
            report = getRepository().saveAndFlush(report);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter
                .hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".")
                    .append(cv.getPropertyPath()).append(": ").append(cv.getMessage())
                    .append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        assertThat(report.getId()).as("Check a 'Report' (%s) id is not null after been persisted", report)
            .isNotNull();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void ReportRepositoryIT.deleteShouldMakeReportUnavailable() {
        // Setup
        Long id = getRandomReportId();
        Report report = getRepository().findOne(id);
        
        // Exercise
        getRepository().delete(report);
        
        // Verify
        assertThat(getRepository().findOne(id))
        .as("Check the deleted 'Report' %s is no longer available with 'findOne'", report).isNull();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void ReportRepositoryIT.findAllCustomNotFilteredNotPagedShouldReturnAllReports() {
        // Exercise
        Page<Report> all = getRepository().findAll((GlobalSearch) null, new PageRequest(0, getDod().getSize()));
        
        // Verify
        assertThat(all.getNumberOfElements())
            .as("Check 'findAll' with null 'GlobalSearch' and no pagination returns all entries")
            .isEqualTo(getDod().getSize());
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void ReportRepositoryIT.findAllCustomNotFilteredPagedShouldReturnAReportsPage() {
        // Exercise
        Page<Report> all = getRepository().findAll((GlobalSearch) null, new PageRequest(0, 3));
        
        // Verify
        assertThat(all.getNumberOfElements())
            .as("Check result number is not greater than the page size").isLessThanOrEqualTo(3);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return Long
     */
    private Long ReportRepositoryIT.getRandomReportId() {
        Report report = getDod().getRandomReport();
        Long id = report.getId();
        assertThat(id).as("Check the Data on demand generated a 'Report' with an identifier").isNotNull();
        return id;
    }
    
}
