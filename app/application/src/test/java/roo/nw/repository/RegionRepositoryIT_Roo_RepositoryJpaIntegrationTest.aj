// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package roo.nw.repository;

import static org.assertj.core.api.Assertions.assertThat;

import io.springlets.data.domain.GlobalSearch;
import java.util.List;
import org.junit.Before;
import org.junit.Rule;
import org.junit.Test;
import org.junit.rules.ExpectedException;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;
import org.springframework.context.annotation.Import;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.PageRequest;
import org.springframework.test.context.junit4.SpringRunner;
import roo.nw.application.config.SpringDataJpaDetachableRepositoryConfiguration;
import roo.nw.model.Region;
import roo.nw.model.dod.DataOnDemandConfiguration;
import roo.nw.model.dod.RegionDataOnDemand;
import roo.nw.repository.RegionRepository;
import roo.nw.repository.RegionRepositoryIT;

privileged aspect RegionRepositoryIT_Roo_RepositoryJpaIntegrationTest {
    
    declare @type: RegionRepositoryIT: @RunWith(SpringRunner.class);
    
    declare @type: RegionRepositoryIT: @DataJpaTest;
    
    declare @type: RegionRepositoryIT: @Import({ DataOnDemandConfiguration.class, SpringDataJpaDetachableRepositoryConfiguration.class });
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    @Rule
    public ExpectedException RegionRepositoryIT.thrown = ExpectedException.none();
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    @Autowired
    private RegionRepository RegionRepositoryIT.repository;
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    @Autowired
    private RegionDataOnDemand RegionRepositoryIT.dod;
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return RegionRepository
     */
    public RegionRepository RegionRepositoryIT.getRepository() {
        return repository;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return RegionDataOnDemand
     */
    public RegionDataOnDemand RegionRepositoryIT.getDod() {
        return dod;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Before
    public void RegionRepositoryIT.checkDataOnDemandHasInitializedCorrectly() {
        assertThat(getDod().getRandomRegion())
            .as("Check data on demand for 'Region' initializes correctly by getting a random Region")
            .isNotNull();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void RegionRepositoryIT.countShouldReturnExpectedValue() {
        // Verify
        assertThat(getRepository().count()).as("Check there are available 'Region' entries").isGreaterThan(0);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void RegionRepositoryIT.findOneShouldReturnExistingRegion() {
        // Setup
        Long id = getRandomRegionId();
        
        // Exercise
        Region region = getRepository().findOne(id);
        
        // Verify
        assertThat(region).as("Check that findOne illegally returned null for id %s", id).isNotNull();
        assertThat(id).as("Check the identifier of the found 'Region' is the same used to look for it")
            .isEqualTo(region.getId());
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void RegionRepositoryIT.findAllShouldReturnAllRegions() {
        // Setup
        assertThat(getRepository().count())
            .as("Check the number of entries is not too big (250 entries). "
                + "If it is, please review the tests so it doesn't take too long to run them")
            .isLessThan(250);
        
        // Exercise
        List<Region> result = getRepository().findAll();
        
        // Verify
        assertThat(result).as("Check 'findAll' returns a not null list of entries").isNotNull();
        assertThat(result.size()).as("Check 'findAll' returns a not empty list of entries")
            .isGreaterThan(0);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void RegionRepositoryIT.findAllCustomNotFilteredNotPagedShouldReturnAllRegions() {
        // Exercise
        Page<Region> all = getRepository().findAll((GlobalSearch) null, new PageRequest(0, getDod().getSize()));
        
        // Verify
        assertThat(all.getNumberOfElements())
            .as("Check 'findAll' with null 'GlobalSearch' and no pagination returns all entries")
            .isEqualTo(getDod().getSize());
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     */
    @Test
    public void RegionRepositoryIT.findAllCustomNotFilteredPagedShouldReturnARegionsPage() {
        // Exercise
        Page<Region> all = getRepository().findAll((GlobalSearch) null, new PageRequest(0, 3));
        
        // Verify
        assertThat(all.getNumberOfElements())
            .as("Check result number is not greater than the page size").isLessThanOrEqualTo(3);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return Long
     */
    private Long RegionRepositoryIT.getRandomRegionId() {
        Region region = getDod().getRandomRegion();
        Long id = region.getId();
        assertThat(id).as("Check the Data on demand generated a 'Region' with an identifier").isNotNull();
        return id;
    }
    
}
